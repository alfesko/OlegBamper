define(['underscore/amd/_createAssigner', 'underscore/amd/allKeys'], function (_createAssigner, allKeys) {

	// Fill in a given object with default properties.
	var defaults = _createAssigner(allKeys, true);

	return defaults;

});
